---
description: Create a well-formatted commit using the git-commit skill
---
I want to commit my changes.
1. Load the `git-commit` skill.
2. Follow its workflow to analyze, stage, and commit changes. Use $ARGUMENTS to identify which files or changes to commit.
3. Propose pushing the successful commits.

NOTE: Always use the Opencode `question` tool and ask one question at a time.

---
description: Lead a deep interview/grilling session on the topic proposed by the user
---
Call the `interview-me` skill with the given context: $ARGUMENTS

When directing the interview process, you may realize that some ideas/concepts are still immature so they must be refined with the `idea-refine` skill.

Depending on what you found, gather all these new informations to clarify the project goals and context inside the `README.md` and `AGENTS.md` files and with some actuable decisions on tools to use and procedures to follow (use the `technical-writing` skill to update these files).
If the goal is clear enough, it may be time to establish a plan with this content and the `spec-driven-development` skill.
